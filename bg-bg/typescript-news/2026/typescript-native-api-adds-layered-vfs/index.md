# Нативният API на TypeScript добавя слоести виртуални файлови системи


**Публикувано:** 9 септември 2026 г.

Нативният API на TypeScript вече може да създава и обновява snapshot-и с изрично зададени данни за виртуална файлова система. Така инструментите могат да моделират добавяне, промяна и премахване на файлове, без да изграждат отново целия вход за файловата система.

## Какво се промени

Новите помощни функции `createFileSystem`, `createFileSystemWithLib` и `createFileSystemLayer` създават VFS обекти, приемани от API-тата за snapshot-и. `Snapshot.update` може да приложи нов кеширащ слой върху съществуващ snapshot.

VFS от тип `full` остава изцяло в паметта и не използва callback-и към host или session файловата система. VFS от тип `layer` се връща към host файловата система при липса в кеша и може да използва `removedPaths`, за да скрива файлове или директории, съществуващи на host-а. И двата вида поддържат символни връзки във виртуалната файлова система и към host пътища.

## Текущо ограничение

Snapshot-ите, поддържани от VFS, все още се считат за реални snapshot-и, затова нативният API запазва текущото ограничение за само един реален snapshot в даден момент. Засега операциите със snapshot-и остават последователни.

## Източник

Прочетете обединената pull request заявка на TypeScript: [Add optional VFS parameters to updateSnapshot](https://github.com/microsoft/TypeScript/pull/64115).
