---
title: any 型
sidebar:
  order: 45
  label: 45. any 型
---


`any` 型は、あらゆる種類の値（プリミティブ、オブジェクト、配列、関数、エラー、シンボル）を表すために使用できる特殊な型（普遍上位型）です。値の型がコンパイル時には不明な場合や、TypeScript の型定義がない外部 API またはライブラリの値を扱う場合によく使用されます。

`any` 型を使用することで、値を一切の制約なしに表すべきだと TypeScript コンパイラへ示します。コードの型安全性を最大限に高めるため、次の点を検討してください。

* `any` の使用は、型が本当に不明な特定のケースに限定します。
* 関数から `any` 型を返さないでください。その関数を使用するコードの型安全性が低下します。
* コンパイラによるチェックを抑制する必要がある場合は、`any` の代わりに `@ts-ignore` を使用します。

```typescript
let value: any;
value = true; // Valid
value = 7; // Valid
```

